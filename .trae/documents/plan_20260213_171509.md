## i18n 汉化计划

### 任务 1: 菜单栏汉化（Rust 原生菜单）
修改 `src-tauri/src/menu.rs` 文件，将所有硬编码的英文菜单文本替换为从翻译文件获取：

**需要汉化的菜单项：**
- 应用菜单：About、Check for Updates、Settings、Services、Hide、Hide Others、Quit
- File 菜单：File、New Agent、New Worktree Agent、New Clone Agent、Add Workspaces、Close Window、Quit
- Edit 菜单：Edit、Undo、Redo、Cut、Copy、Paste、Select All
- Composer 菜单：Composer、Cycle Model、Cycle Access Mode、Cycle Reasoning Mode、Cycle Collaboration Mode
- View 菜单：View、Toggle Projects Sidebar、Toggle Git Sidebar、Toggle Debug Panel、Toggle Terminal、Next Agent、Previous Agent、Next Workspace、Previous Workspace、Toggle Full Screen
- Window 菜单：Window、Minimize、Maximize、Close Window
- Help 菜单：Help、About

**实现方式：**
- 在 Rust 端通过 Tauri 事件从前端获取翻译后的菜单文本
- 或者在 Rust 端实现简单的 i18n 映射

### 任务 2: Server 设置页面汉化
修改 `src/features/settings/components/sections/SettingsServerSection.tsx`，将所有硬编码英文替换为 `t()` 函数调用：

**需要添加的翻译键（约 40+ 个）：**
- server.subtitle_mobile / server.subtitle_desktop
- server.connection_type / server.remote_provider
- server.keep_daemon_running / server.keep_daemon_running_subtitle
- server.mobile_access_daemon / server.start_daemon / server.stop_daemon
- server.tailscale_helper / server.detect_tailscale
- server.orbit_websocket_url / server.orbit_auth_url
- 等等

### 任务 3: 更新翻译文件
在 `src/i18n/locales/zh/translation.json` 和 `src/i18n/locales/en/translation.json` 中添加对应的翻译键值对。